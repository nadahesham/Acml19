version: '3'
services:
  app:
    container_name: docker-node-mongo-acml
    restart: always
    build: .
    ports:
      - '3000:5000'
    links:
      - mongo
  mongo:
    container_name: mongo
    image: mongo
    ports:
      - '27017:27017'      